# Step 4: Validation Instructions

**Purpose:** Validate specifications for completeness and feasibility.

**Input file:**
- `input.yaml` (copy/reference from 3_specification/output.yaml)
- See `example/input.yaml` for a sample input for this step.

**Output files:**
- `output.yaml` (validation results and feedback)
- See `example/output.yaml` for a sample output for this step.

- Plan and execute validation, recording results in `validation_report.md`.
- See `output_example.yaml` for an example output for this step.
- See [../instructions.md](../instructions.md) for the full pipeline overview.
