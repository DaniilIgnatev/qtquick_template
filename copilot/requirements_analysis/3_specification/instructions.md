# Step 3: Specification Instructions

**Purpose:** Translate structured requirements into actionable specifications.

**Input file:**
- `input.yaml` (copy/reference from 2_analysis/output.yaml)
- See `example/input.yaml` for a sample input for this step.

**Output file:**
- `output.yaml` (detailed functional and non-functional specifications, acceptance criteria)
- See `example/output.yaml` for a sample output for this step.

	- Write detailed functional and non-functional specifications in `output.yaml` (YAML format).
	- Define acceptance criteria in YAML.
	- See `example/output.yaml` for an example output for this step.
	- See [../instructions.md](../instructions.md) for the full pipeline overview.
