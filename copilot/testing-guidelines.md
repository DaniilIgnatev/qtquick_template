## Testing

Unit tests cover both QML/JS and C++ (UI and logic).

Set up and keep up unit tests for:
- Functionality that is prone to changes
- Previously failed subtask
- Non-trivial logic
- Dynamic UI
