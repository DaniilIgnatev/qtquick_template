import QtQuick 2.15
import QtQuick.Controls 2.15

Rectangle {
    color: "#c8f7c5"
    anchors.fill: parent

    Text {
        text: "This is QML via QQuickView (QWindow)"
        anchors.centerIn: parent
        font.pixelSize: 18
    }
}
